import { useEffect, useMemo, useRef, useState } from 'react';
import { App, Button, Card, Checkbox, Drawer, Empty, Input, InputNumber, Modal, Segmented, Space, Tag, Tooltip } from 'antd';
import { CheckCircleOutlined, CloseOutlined, CreditCardOutlined, InfoCircleOutlined, PlusOutlined, SaveOutlined, SearchOutlined, ShoppingCartOutlined } from '@ant-design/icons';
import SmartFieldRenderer from '../SmartFieldRenderer';
import QrScanPopover from '../QrScanPopover';
import { invoicesConfig } from '../../modules/invoicesConfig';
import { customerModule } from '../../modules/customerConfig';
import { supabase } from '../../supabaseClient';
import { applyInvoiceFinalizationInventory } from '../../utils/invoiceInventoryWorkflow';
import { getTodayLocalDateValue } from '../../utils/defaultValues';
import { buildClientFallbackSystemCode } from '../../utils/systemCode';
import { getCachedAuthUser, fetchSessionBootstrap } from '../../utils/sessionCache';

type Item = { key: string; id: string; source: 'products' | 'product_bundles'; name: string; image_url?: string; unit_price: number; quantity: number; main_unit?: string; stock?: number | null };
type Cart = { id: string; title: string; status: string; general: boolean; customer_id?: string; mobile?: string; prefix?: string; first_name?: string; last_name?: string; items: Item[]; payment?: { amount: number; type: string }; successId?: string };
const makeCart = (): Cart => ({ id: crypto.randomUUID(), title: 'فروش جدید', status: 'final', general: false, items: [] });
const n = (value: any) => Number(value || 0) || 0;
const field = (key: string) => (invoicesConfig.fields.find((item) => item.key === key) as any);
const customerField = (key: string) => (customerModule.fields.find((item) => item.key === key) as any);

export default function RetailInvoiceWorkspace({ onClose }: { onClose: () => void }) {
  const { message } = App.useApp(); const searchRef = useRef<any>(null);
  const [carts, setCarts] = useState<Cart[]>([makeCart()]); const [activeId, setActiveId] = useState(''); const [catalog, setCatalog] = useState<any[]>([]); const [query, setQuery] = useState(''); const [paymentOpen, setPaymentOpen] = useState(false); const [detailsOpen, setDetailsOpen] = useState(false); const [helpOpen, setHelpOpen] = useState(false); const [saving, setSaving] = useState(false); const [ready, setReady] = useState(false);
  const cart = carts.find((item) => item.id === activeId) || carts[0];
  const update = (patch: Partial<Cart>) => setCarts((all) => all.map((item) => item.id === cart.id ? { ...item, ...patch } : item));
  const total = useMemo(() => (cart?.items || []).reduce((sum, item) => sum + n(item.quantity) * n(item.unit_price), 0), [cart]);
  const unitSummary = useMemo(() => Object.entries((cart?.items || []).reduce<Record<string, number>>((all, item) => ({ ...all, [item.main_unit || 'واحد']: n(all[item.main_unit || 'واحد']) + n(item.quantity) }), {})), [cart]);

  useEffect(() => { let alive = true; void (async () => { const session = await fetchSessionBootstrap(supabase, { force: true }); const user = await getCachedAuthUser(supabase); const key = `kalamapp:retail-invoice:v1:${session.orgId}:${user?.id}`; try { const saved = JSON.parse(sessionStorage.getItem(key) || ''); if (saved.carts?.length) { setCarts(saved.carts); setActiveId(saved.activeId || saved.carts[0].id); } else setActiveId(carts[0].id); } catch { setActiveId(carts[0].id); } finally { if (alive) setReady(true); } })(); return () => { alive = false; }; }, []);
  useEffect(() => { if (!ready) return; void (async () => { const session = await fetchSessionBootstrap(supabase); const user = await getCachedAuthUser(supabase); sessionStorage.setItem(`kalamapp:retail-invoice:v1:${session.orgId}:${user?.id}`, JSON.stringify({ carts, activeId })); })(); }, [carts, activeId, ready]);
  useEffect(() => { const timer = window.setTimeout(() => void loadCatalog(), 220); return () => window.clearTimeout(timer); }, [query]);
  useEffect(() => { const onKey = (event: KeyboardEvent) => { if (event.ctrlKey && event.key === 'Enter') { event.preventDefault(); void save(); } else if (event.ctrlKey && event.shiftKey && event.key.toLowerCase() === 'n') { event.preventDefault(); addCart(); } else if (event.altKey && event.key.toLowerCase() === 'p') { event.preventDefault(); setPaymentOpen(true); } else if (event.altKey && event.key.toLowerCase() === 'f') { event.preventDefault(); searchRef.current?.focus(); } else if (event.key === 'Escape') { setDetailsOpen(false); setPaymentOpen(false); } }; window.addEventListener('keydown', onKey); return () => window.removeEventListener('keydown', onKey); });
  useEffect(() => { const media = window.matchMedia('(max-width: 767px)'); const sync = () => { document.querySelectorAll('kbd').forEach((node) => { (node as HTMLElement).style.display = media.matches ? 'none' : ''; }); document.querySelectorAll('.anticon-info-circle').forEach((icon) => { const button = icon.closest('button') as HTMLElement | null; if (button) button.style.display = media.matches ? 'none' : ''; }); }; sync(); media.addEventListener('change', sync); return () => { media.removeEventListener('change', sync); document.querySelectorAll('kbd').forEach((node) => { (node as HTMLElement).style.display = ''; }); }; }, []);
  const loadCatalog = async () => { const text = query.trim(); const filter = (q: any) => text ? q.ilike('name', `%${text}%`) : q; const [products, bundles] = await Promise.all([filter(supabase.from('products').select('id,name,image_url,sell_price,stock,main_unit,retail_quick_add_enabled,retail_display_order').eq('status', 'active').order('retail_quick_add_enabled', { ascending: false }).order('retail_display_order', { ascending: true, nullsFirst: false }).order('name').limit(40)), filter(supabase.from('product_bundles').select('id,name,image_url,sell_price,retail_quick_add_enabled,retail_display_order').eq('status', 'active').order('retail_quick_add_enabled', { ascending: false }).order('retail_display_order', { ascending: true, nullsFirst: false }).order('name').limit(40))]); setCatalog([...(products.data || []).map((item: any) => ({ ...item, source: 'products' })), ...(bundles.data || []).map((item: any) => ({ ...item, source: 'product_bundles' }))]); };
  const addCart = () => { const next = makeCart(); setCarts((all) => [...all, next]); setActiveId(next.id); };
  const addItem = (row: any) => { const found = cart.items.find((item) => item.id === row.id && item.source === row.source); update({ items: found ? cart.items.map((item) => item === found ? { ...item, quantity: item.quantity + 1 } : item) : [...cart.items, { key: crypto.randomUUID(), id: row.id, source: row.source, name: row.name, image_url: row.image_url, unit_price: n(row.sell_price), quantity: 1, main_unit: row.main_unit, stock: row.stock }] }); };
  const scan = async (result: any) => { const source = result.moduleId === 'product_bundles' ? 'product_bundles' : 'products'; if (!result.recordId) return setQuery(result.raw); const { data } = await supabase.from(source).select('id,name,image_url,sell_price,stock,main_unit').eq('id', result.recordId).maybeSingle(); if (!data) return message.warning('محصول اسکن‌شده یافت نشد.'); setQuery(data.name); setCatalog([{ ...data, source }]); };
  const findCustomer = async () => { const value = String(cart.mobile || '').replace(/\D/g, ''); if (value.length < 8) return; const { data } = await supabase.from('customers').select('id,full_name,mobile_1').ilike('mobile_1', `%${value}%`).limit(3); if (data?.length === 1) { update({ customer_id: data[0].id }); message.success(`مشتری «${data[0].full_name}» انتخاب شد.`); } };
  const save = async () => { if (!cart.items.length) return message.warning('حداقل یک قلم انتخاب کنید.'); setSaving(true); try { let customerId = cart.customer_id; if (cart.general) { const { data, error } = await supabase.rpc('ensure_retail_general_customer'); if (error) throw error; customerId = String(data); } else if (!customerId) { if (!cart.first_name?.trim() || !cart.last_name?.trim()) throw new Error('نام و نام خانوادگی مشتری را کامل کنید.'); const { data, error } = await supabase.from('customers').insert({ prefix: cart.prefix || null, first_name: cart.first_name, last_name: cart.last_name, full_name: [cart.prefix, cart.first_name, cart.last_name].filter(Boolean).join(' '), mobile_1: cart.mobile || null }).select('id').single(); if (error) throw error; customerId = data.id; }
    const invoiceItems = cart.items.map((item) => ({ product_id: item.source === 'products' ? item.id : null, package_id: item.source === 'product_bundles' ? item.id : null, quantity: item.quantity, unit_price: item.unit_price, total_price: item.quantity * item.unit_price, main_unit: item.main_unit })); const user = await getCachedAuthUser(supabase); const { data, error } = await supabase.from('invoices').insert({ name: 'فروش فروشگاهی', system_code: await buildClientFallbackSystemCode(supabase, 'invoices', 'invoices'), customer_id: customerId, status: cart.status, invoice_date: getTodayLocalDateValue(), invoiceItems, payments: cart.payment?.amount ? [{ amount: cart.payment.amount, payment_type: cart.payment.type, date: getTodayLocalDateValue() }] : [] }).select('id').single(); if (error) throw error; await applyInvoiceFinalizationInventory({ supabase: supabase as any, moduleId: 'invoices', recordId: data.id, previousStatus: null, nextStatus: cart.status, invoiceItems, userId: user?.id || null }); update({ successId: data.id }); } catch (error: any) { message.error(String(error.message || 'ثبت فاکتور ناموفق بود.')); } finally { setSaving(false); } };
  if (!ready || !cart) return <div className="p-10 text-center">در حال آماده‌سازی…</div>;
  if (cart.successId) return <Card className="mx-auto mt-10 max-w-lg text-center"><CheckCircleOutlined className="text-4xl text-emerald-500" /><h2>فاکتور با موفقیت ثبت شد</h2><Space><Button type="primary" href={`/invoices/${cart.successId}`}>مشاهده فاکتور</Button><Button onClick={() => { setCarts((all) => all.filter((item) => item.id !== cart.id)); setActiveId(carts.find((item) => item.id !== cart.id)?.id || ''); }}>بستن تب</Button></Space></Card>;
  const lineSummary = <div className="space-y-2">{cart.items.map((item) => <div key={item.key} className="rounded-xl bg-slate-50 p-2 dark:bg-white/5"><div className="flex items-center justify-between gap-2"><b className="truncate">{item.name}</b><Button type="text" size="small" icon={<CloseOutlined />} onClick={() => update({ items: cart.items.filter((row) => row.key !== item.key) })} /></div><div className="mt-2 grid grid-cols-2 gap-2"><InputNumber value={item.quantity} min={0.001} onChange={(quantity) => update({ items: cart.items.map((row) => row.key === item.key ? { ...row, quantity: n(quantity) } : row) })} /><InputNumber value={item.unit_price} min={0} onChange={(unit_price) => update({ items: cart.items.map((row) => row.key === item.key ? { ...row, unit_price: n(unit_price) } : row) })} /></div></div>)}</div>;
  return <div dir="rtl" className="mx-auto max-w-[1600px] pb-28 md:p-5"><header className="mb-4 rounded-2xl border border-blue-100 bg-gradient-to-l from-blue-50 to-white p-3 shadow-sm dark:border-blue-900 dark:from-blue-950/30 dark:to-slate-950"><div className="flex flex-wrap items-center gap-2"><Segmented value={activeId} options={carts.map((item) => ({ value: item.id, label: item.title }))} onChange={(value) => setActiveId(String(value))} /><Button type="text" icon={<PlusOutlined />} onClick={addCart}>تب تازه <kbd>Ctrl+Shift+N</kbd></Button><div className="mr-auto"><Tooltip title="راهنمای میانبرها"><Button type="text" icon={<InfoCircleOutlined />} onClick={() => setHelpOpen(true)} /></Tooltip><Button type="text" onClick={onClose}>حالت عادی</Button></div></div><div className="mt-3 grid gap-2 md:grid-cols-5"><div className="md:col-span-1"><Checkbox checked={cart.general} onChange={(event) => update({ general: event.target.checked, customer_id: undefined })}>ثبت به‌عنوان مشتری عمومی</Checkbox></div>{!cart.general ? <><Input placeholder="شماره تماس مشتری" value={cart.mobile} onBlur={() => void findCustomer()} onChange={(event) => update({ mobile: event.target.value, customer_id: undefined })} /><SmartFieldRenderer field={customerField('prefix')} value={cart.prefix} onChange={(prefix) => update({ prefix })} allValues={cart} moduleId="customers" standalone forceEditMode /><SmartFieldRenderer field={customerField('first_name')} value={cart.first_name} onChange={(first_name) => update({ first_name })} allValues={cart} moduleId="customers" standalone forceEditMode /><SmartFieldRenderer field={customerField('last_name')} value={cart.last_name} onChange={(last_name) => update({ last_name })} allValues={cart} moduleId="customers" standalone forceEditMode /></> : null}<SmartFieldRenderer field={field('status')} value={cart.status} onChange={(status) => update({ status })} allValues={cart} moduleId="invoices" standalone forceEditMode /></div></header><div className="grid gap-4 lg:grid-cols-[minmax(0,1fr)_370px]"><main><Card className="rounded-2xl" title="انتخاب کالا و پکیج"><div className="flex gap-2"><Input ref={searchRef} prefix={<SearchOutlined />} value={query} placeholder="جست‌وجوی کالا یا پکیج (Alt+F)" onChange={(event) => setQuery(event.target.value)} /><QrScanPopover label="اسکن QR" onScan={(result) => void scan(result)} /></div><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-3 xl:grid-cols-4">{catalog.map((row) => <button type="button" key={`${row.source}-${row.id}`} className="min-h-36 rounded-2xl border bg-white p-3 text-right shadow-sm transition hover:border-blue-500 hover:shadow-md dark:bg-slate-900" onClick={() => addItem(row)}><b className="block truncate">{row.name}</b><Tag className="mt-2" color={row.source === 'products' ? 'blue' : 'cyan'}>{row.source === 'products' ? 'کالا/خدمت' : 'پکیج'}</Tag><div className="mt-2 font-black text-blue-700">{n(row.sell_price).toLocaleString('fa-IR')}</div>{row.stock != null ? <small>موجودی: {row.stock}</small> : null}</button>)}</div>{!catalog.length ? <Empty description="قلمی یافت نشد" /> : null}</Card></main><aside className="hidden lg:block"><Card className="sticky top-4 rounded-2xl" title="خلاصه فاکتور">{lineSummary}<div className="mt-4 flex justify-between text-lg font-black"><span>جمع کل</span><span>{total.toLocaleString('fa-IR')}</span></div><Button block className="mt-3" icon={<CreditCardOutlined />} onClick={() => setPaymentOpen(true)}>ثبت دریافت <kbd>Alt+P</kbd></Button><Button block type="primary" className="mt-2" icon={<SaveOutlined />} loading={saving} onClick={() => void save()}>ذخیره فاکتور <kbd>Ctrl+Enter</kbd></Button></Card></aside></div><div className="fixed bottom-[calc(var(--app-mobile-footer-height,64px)+env(safe-area-inset-bottom)+8px)] left-2 right-2 z-[999] rounded-2xl border bg-white/95 p-2 shadow-2xl backdrop-blur lg:hidden dark:bg-slate-900/95"><div className="flex items-center justify-between text-xs">{unitSummary.map(([unit, amount]) => <span key={unit}>{amount} {unit}</span>)}<b>{total.toLocaleString('fa-IR')}</b></div><div className="mt-2 grid grid-cols-3 gap-2"><Button onClick={() => setDetailsOpen(true)}>مشاهده بیشتر</Button><Button icon={<CreditCardOutlined />} onClick={() => setPaymentOpen(true)}>دریافت</Button><Button type="primary" icon={<SaveOutlined />} loading={saving} onClick={() => void save()}>ذخیره</Button></div></div><Drawer title="اقلام و اطلاعات فاکتور" placement="bottom" height="75vh" open={detailsOpen} onClose={() => setDetailsOpen(false)}>{lineSummary}</Drawer><Modal title="ثبت دریافت سریع" open={paymentOpen} onCancel={() => setPaymentOpen(false)} onOk={() => setPaymentOpen(false)}><InputNumber className="w-full" placeholder="مبلغ دریافت" value={cart.payment?.amount} onChange={(amount) => update({ payment: { amount: n(amount), type: cart.payment?.type || 'cash' } })} /><Input className="mt-3" value={cart.payment?.type || 'cash'} onChange={(event) => update({ payment: { amount: cart.payment?.amount || 0, type: event.target.value } })} /></Modal><Modal title="میانبرها" open={helpOpen} footer={null} onCancel={() => setHelpOpen(false)}><p>Ctrl+Enter ذخیره فاکتور</p><p>Ctrl+Shift+N تب جدید</p><p>Alt+P ثبت دریافت</p><p>Alt+F جست‌وجوی کالا</p></Modal></div>;
}
