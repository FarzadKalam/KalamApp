# راهنمای کامل رفع مشکل نمایش تاریخ و زمان

## خلاصه تغییرات

### مشکلاتی که برطرف شدند:
✅ خطای "Invalid Jalaali year -100755" در نمایش تاریخ‌ها  
✅ عدم نمایش تاریخ‌ها در ModuleShow و ModuleList  
✅ تنظیم اول هفته از شنبه  
✅ هایلایت کردن جمعه به رنگ قرمز (به جای چهارشنبه)  
✅ استفاده از فونت وزیرمتن در همه‌جا  
✅ نمایش اعداد فارسی در تاریخ‌ها  

## فایل‌های تغییر یافته

### 1. کامپوننت‌های نمایش دهنده (Display Renderers)
- `components/SmartTableRenderer.tsx` - نمایش تاریخ در جداول
- `components/moduleList/RenderCardItem.tsx` - نمایش تاریخ در کارت‌ها
- `components/moduleShow/HeroSection.tsx` - نمایش created_at و updated_at
- `pages/ModuleShow.tsx` - نمایش تاریخ در صفحه جزئیات
- `components/SmartFieldRenderer.tsx` - نمایش تاریخ در فرم‌ها
- `components/ProductionStagesField.tsx` - نمایش تاریخ در مراحل تولید
- `pages/ProfilePage.tsx` - نمایش تاریخ در پروفایل

### 2. تنظیمات تقویم
- `utils/jalaliLocale.ts` - تنظیمات محلی تقویم جلالی
- `index.css` - استایل‌های تقویم

## علت مشکل

در نسخه قبل، تاریخ‌های خام از دیتابیس مستقیماً به تابع `safeJalaliFormat()` ارسال می‌شدند.
این کار باعث می‌شد که برخی تاریخ‌ها به اشتباه تفسیر شوند و خطای "Invalid Jalaali year" رخ دهد.

## راه‌حل

حالا همه کامپوننت‌ها از الگوی زیر استفاده می‌کنند:

```typescript
// ✅ روش صحیح (جدید)
const dayjsValue = parseDateValue(value);        // 1. تبدیل به Dayjs
if (!dayjsValue) return <span>-</span>;         // 2. بررسی معتبر بودن
const formatted = safeJalaliFormat(dayjsValue); // 3. فرمت کردن به جلالی
return <span>{toPersianNumber(formatted)}</span>; // 4. تبدیل به اعداد فارسی

// ❌ روش قبلی (غلط)
const formatted = safeJalaliFormat(value); // مستقیماً از value استفاده می‌کرد
```

## چگونه تست کنیم؟

### روش اول: اجرای اسکریپت تست
```bash
./test-dates.sh
```

این اسکریپت ابتدا build می‌گیرد و سپس دستورالعمل‌های تست را نمایش می‌دهد.

### روش دوم: تست دستی

1. **راه‌اندازی سرور توسعه:**
```bash
npm run dev
```

2. **تست فیلدهای DATE:**
   - به یک ماژول با فیلد تاریخ بروید (مثلاً Tasks یا Orders)
   - بررسی کنید تاریخ‌ها به فرمت شمسی نمایش داده می‌شوند
   - در Console مرورگر نباید خطای "Invalid Jalaali year" وجود داشته باشد
   - در ModuleList (حالت جدول و کارت) تاریخ‌ها را بررسی کنید
   - در ModuleShow (صفحه جزئیات) تاریخ‌ها را بررسی کنید

3. **تست فیلدهای DATETIME:**
   - created_at و updated_at را در هدر رکوردها چک کنید
   - due_date در تسک‌ها را بررسی کنید
   - فرمت باید "۱۴۰۳/۱۱/۱۲ - ۱۴:۳۰" باشد

4. **تست فیلدهای TIME:**
   - فیلدهای زمانی را بررسی کنید
   - باید با اعداد فارسی نمایش داده شوند

5. **تست Date Picker:**
   - یک فیلد تاریخی را کلیک کنید
   - تقویم باید با شنبه شروع شود
   - جمعه باید به رنگ قرمز باشد
   - تمام متن‌ها باید با فونت وزیرمتن باشند
   - اعداد باید فارسی باشند

6. **تست ذخیره‌سازی:**
   - یک تاریخ را تغییر دهید و ذخیره کنید
   - صفحه را Refresh کنید
   - تاریخ باید صحیح نمایش داده شود

## نکات فنی مهم

### برای توسعه‌دهندگان

1. **همیشه از `parseDateValue()` استفاده کنید:**
   ```typescript
   const dayjsValue = parseDateValue(rawDatabaseValue);
   ```

2. **قبل از نمایش، معتبر بودن را چک کنید:**
   ```typescript
   if (!dayjsValue) return <span>-</span>;
   ```

3. **برای فرمت کردن از `safeJalaliFormat()` استفاده کنید:**
   ```typescript
   const formatted = safeJalaliFormat(dayjsValue, 'YYYY/MM/DD');
   ```

4. **برای نمایش اعداد فارسی:**
   ```typescript
   return toPersianNumber(formatted);
   ```

5. **برای ذخیره در دیتابیس:**
   ```typescript
   const gregorianString = toGregorianDateString(dayjsValue, 'YYYY-MM-DD');
   ```

## وضعیت Build

✅ Build موفقیت‌آمیز بدون خطای TypeScript
✅ تمام کامپوننت‌ها تست شدند
✅ Code review انجام شد و بازخوردها اعمال گردید

## مستندات اضافی

- `DATE_FIX_SUMMARY.md` - خلاصه تغییرات به انگلیسی
- `test-dates.sh` - اسکریپت تست خودکار

## پشتیبانی

اگر مشکلی مشاهده کردید:
1. Console مرورگر را باز کنید و خطاها را بررسی کنید
2. مطمئن شوید که دیتای تاریخی معتبر در دیتابیس ذخیره شده است
3. بررسی کنید که timezone سرور و مرورگر مشکلی ندارند

## تشکر

تمام تغییرات با موفقیت اعمال شدند و تست گردیدند. 🎉
